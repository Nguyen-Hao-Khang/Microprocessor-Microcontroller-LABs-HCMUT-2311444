/* USER CODE BEGIN PV */
uint16_t ledPins[12] = {
	  GPIO_PIN_4,
	  GPIO_PIN_5,
	  GPIO_PIN_6,
	  GPIO_PIN_7,
	  GPIO_PIN_8,
	  GPIO_PIN_9,
	  GPIO_PIN_10,
	  GPIO_PIN_11,
	  GPIO_PIN_12,
	  GPIO_PIN_13,
	  GPIO_PIN_14,
	  GPIO_PIN_15,
};
/* USER CODE END PV */


  int count = 0;
  while (1)
  {
	  if (count >= 12) count = 0;
	  HAL_GPIO_WritePin(GPIOA, ledPins[count++], SET);
      HAL_Delay(100);
  }